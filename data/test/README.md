### Test Data

This folder contains test datasets for the `branded_food_product` and `FoodKG` tables.

### Purpose

The `FoodKG` dataset was manually annotated and includes 100 rows of test data mapping recipes to their base
ingredients.

The `branded_food_product` dataset was also manually annotated and includes 300 rows of test data mapping each product
to its base ingredient.
